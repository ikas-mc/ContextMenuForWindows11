<UserControl
    x:Class="ContextMenuCustomApp.View.Menu.MenuFileRenameDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBox
            x:Name="FileNameTextBox"
            Grid.Row="0"
            Margin="0,8"
            Header="{x:Bind _appLang.MenuFileRenameInputTips}" />
        <Button
            Grid.Row="1"
            HorizontalAlignment="Right"
            Click="Button_Click" Content="{x:Bind _appLang.MenuFileRenameSyncName}">
        </Button>
    </Grid>
</UserControl>
